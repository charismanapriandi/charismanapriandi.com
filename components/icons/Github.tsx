import { LogoGithub } from '@emotion-icons/ionicons-solid/LogoGithub'
import styled from '@emotion/styled'

const Github = styled(LogoGithub)(({theme}) => ({
  color: theme.palette.text.primary,
}))

export default Github;